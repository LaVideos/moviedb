import {API_KEY,Base_Url} from "@/constants/env.process";
import {headers} from "@/constants/headers";
import {ENDPOINTS} from "@/constants/endpoints";
import {posterURL_original,posterURL_p} from "@/constants/urls";

export {
    API_KEY,
    headers,
    ENDPOINTS,Base_Url,posterURL_original,posterURL_p
}
